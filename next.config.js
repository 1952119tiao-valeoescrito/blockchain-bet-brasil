/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Adicione outras configurações específicas do seu projeto aqui, se precisar
  // Exemplo: images, env, webpack, etc.
};

module.exports = nextConfig;