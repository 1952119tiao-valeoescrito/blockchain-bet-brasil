// src/components/layout/ConnectButton.tsx - A VERSÃO FINAL E CORRETA

'use client';

// Importamos o botão CERTO, da biblioteca CERTA (RainbowKit).
import { ConnectButton } from '@rainbow-me/rainbowkit';

export default function CustomConnectButton() {
  // E aqui a gente usa ele. Sem segredos.
  return <ConnectButton />;
}